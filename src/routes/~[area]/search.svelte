<script>
  // `/search?category=apt&topic=xxx
  export async function preload(page, session) {
    console.log("page:");
    console.log(page);

    console.log("session:");
    console.log(session);
  }
</script>

<h1>Search here.</h1>
